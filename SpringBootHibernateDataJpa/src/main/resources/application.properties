spring.datasource.url=jdbc:oracle:thin:@localhost:1521/ORCL
##spring.datasource.username=c##arjun
spring.datasource.username=sys as sysdba
spring.datasource.password=kingofkings
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.Oracle12cDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql = true

# The below properties are used only, when creating table or insert data into the table manually. No need of using Entity class or Nothing required.
#spring.sql.init.mode=always
#spring.sql.init.schema-locations=classpath:schema.sql
#spring.sql.init.data-locations=classpath:data.sql
